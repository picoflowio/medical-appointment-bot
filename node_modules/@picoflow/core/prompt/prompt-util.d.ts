export declare class Prompt {
    private static cache;
    static file(filePath: string): string;
    private static getCallerDir;
    static set(prompt: string, key: string, value: string): string;
    static replace(template: string, values: Record<string, string>): string;
}
