import { Flow } from './flow';
import { ClassType, LogicResponseType } from './flow-types';
import { Step } from './step';
export declare abstract class LogicStep extends Step {
    constructor(step: ClassType, flow: Flow, isActive?: boolean);
    isLogic(): boolean;
    abstract runLogic(): Promise<LogicResponseType>;
}
