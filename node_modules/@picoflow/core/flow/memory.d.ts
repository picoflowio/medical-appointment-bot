import { MemoryType } from '../flow/flow-types';
import { MessageTypes } from '../utils/message-util';
export declare class Memory {
    private nameSpaceMap;
    constructor(nameSpaces?: string[]);
    getMessages(nameSpace: string): MessageTypes[];
    createOoc(): MemoryType[];
    readDoc(memDocs: MemoryType[]): Memory;
    writeDoc(memDocs: MemoryType[]): void;
}
