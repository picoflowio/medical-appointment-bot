import { MessageContent } from '@langchain/core/messages';
import { Flow } from './flow';
import { MessageTypes } from '../utils/message-util';
export declare class LlmRunner {
    static send(flow: Flow, langMessage?: MessageTypes, prior?: MessageContent): Promise<MessageContent>;
    private static onResponse;
    private static onToolResponseMessages;
    private static callTool;
    private static isBotMessageEmpty;
    private static getReason;
}
