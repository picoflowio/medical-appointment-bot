import { LoggerService } from '@nestjs/common';
import { SessionType } from '../flow/flow-types';
export declare class SessionLogger implements LoggerService {
    private sessionDoc;
    constructor(sessionDoc: SessionType);
    private writeLog;
    log(message: string, tree?: object): void;
    error(message: string, tree?: object): void;
    warn(message: string, tree?: object): void;
    debug(message: string, tree?: object): void;
    verbose(message: string, tree?: object): void;
}
