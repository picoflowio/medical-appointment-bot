import { RunResponseType, SessionType } from '../flow/flow-types';
export declare class FlowSession {
    private adaptor;
    constructor();
    fetch(sessionId: string): Promise<[boolean, SessionType]>;
    fetchAll(sessionId: string): Promise<SessionType>;
    restore(sessionId: string): Promise<SessionType>;
    create(): Promise<SessionType>;
    save(sessionDoc: SessionType): Promise<any>;
    delete(sessionId: string): Promise<any>;
    static handleException(flowSession: FlowSession, sessionId: string, error: Error): Promise<RunResponseType>;
    static endChat(flowSession: FlowSession, sessionId: string): Promise<{
        success: boolean;
    }>;
}
