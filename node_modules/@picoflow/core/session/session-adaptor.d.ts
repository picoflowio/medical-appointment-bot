import { SessionType } from '../flow/flow-types';
export declare abstract class SessionAdaptor {
    abstract fetch(sessionId: string): Promise<[boolean, SessionType]>;
    abstract fetchAll(sessionId: string): Promise<SessionType>;
    abstract restore(sessionId: string): Promise<SessionType>;
    abstract delete(sessionId: string): any;
    abstract create(): Promise<SessionType>;
    abstract save(sessionDoc: SessionType): any;
}
