import { SessionType } from '../flow/flow-types';
import { SessionAdaptor } from './session-adaptor';
import { Container } from '@azure/cosmos';
export declare class CosmoSession extends SessionAdaptor {
    private client;
    constructor();
    fetch(sessionId: string): Promise<[boolean, SessionType]>;
    fetchAll(sessionId: string): Promise<SessionType>;
    restore(sessionId: string): Promise<SessionType>;
    create(): Promise<SessionType>;
    save(sessionDoc: SessionType): Promise<void>;
    delete(sessionId: string): Promise<void>;
    getContainer(): Promise<Container>;
}
