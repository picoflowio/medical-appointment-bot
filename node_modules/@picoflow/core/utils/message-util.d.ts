import { AIMessage, HumanMessage, SystemMessage, ToolMessage } from '@langchain/core/messages';
import { ToolStatus } from '../flow/flow-types';
import { Step } from '../flow/step';
import { ToolCall } from '@langchain/core/messages/tool';
export type MessageTypes = AIMessage | HumanMessage | SystemMessage | ToolMessage;
export type MessageHint = {
    stopTool?: boolean;
    direct?: boolean;
};
export declare class MessageUtil {
    static isSame(message1: MessageTypes, message2: MessageTypes): boolean;
    static isEmptyObject(obj: Record<string, any>): boolean;
    static messageStep(messageId: string): string;
    static messageTime(messageId: string): string;
    static replaceStep(message: MessageTypes, step: Step): void;
    static peek(messages: MessageTypes[]): MessageTypes;
    static makeToolStruct(toolCalls: ToolCall[]): ToolCall[];
}
export declare class AiMessageEx extends AIMessage {
    constructor(step: Step, content: string, hint?: MessageHint);
}
export declare class HumanMessageEx extends HumanMessage {
    constructor(step: Step, content: string, hint?: MessageHint);
}
export declare class ToolMessageEx extends ToolMessage {
    constructor(step: Step, tool: ToolCall, content: string, status?: ToolStatus, hint?: MessageHint);
}
export declare class ToolCallMessage extends AIMessage {
    constructor(step: Step, toolCalls: ToolCall[]);
}
export declare class ToolMessageOK extends ToolMessageEx {
    constructor(step: Step, tool: ToolCall);
}
export declare class ToolMessageBad extends ToolMessageEx {
    constructor(step: Step, tool: ToolCall, content: string);
}
export declare class ToolMessageInfo extends ToolMessageEx {
    constructor(step: Step, tool: ToolCall, content: string);
}
export declare class ToolCallEx implements ToolCall {
    readonly type?: 'tool_call';
    name: string;
    args: Record<string, any>;
    id: string;
    step: string;
    constructor(name: string, args: Record<string, any>, step: Step);
}
export declare class DirectMessage extends AiMessageEx {
    constructor(step: Step, content: string | object);
}
