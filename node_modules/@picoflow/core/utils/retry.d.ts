export declare class Retry {
    static goTry<T>({ maxRetries, delayMs, exponentialBackoff, onMain, onWarn, shouldRetry, onError, onFinalError, }: {
        maxRetries: number;
        delayMs: number;
        exponentialBackoff?: boolean;
        onMain: () => Promise<T>;
        onWarn: (attempts: number, maxRetries: number, error: Error) => void;
        shouldRetry: (result: T, attempts: number) => boolean;
        onFinalError: (error: Error) => void;
        onError?: (error: Error) => void;
    }): Promise<T>;
    private static delay;
}
