export declare enum LLMType {
    OPENAI = "openai",
    GEMINI = "gemini",
    UNSUPPORTED = "unsupported"
}
type FileContentPart = {
    llm: LLMType;
    contentPart: any;
    cleanup?: () => Promise<void>;
};
export declare class LLMFileManager {
    private openaiClient;
    private geminiClient;
    private llm;
    constructor(llm: LLMType);
    private init;
    uploadFile(file: string | Buffer, mimeType?: string): Promise<FileContentPart>;
    private getMimeType;
    private uploadToOpenAI;
    private uploadToGemini;
}
export {};
