import { AIMessageChunk } from '@langchain/core/messages';
export declare class ToolUtil {
    static hasTool(response: AIMessageChunk): boolean;
    static hasNoTool(response: AIMessageChunk): boolean;
}
