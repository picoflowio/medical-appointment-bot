export declare class StringUtil {
    static stripNewlines(input: string): string;
    static stripJson(input: string): string | null;
    static stripQuote(input: string): string | null;
    static parseJson<T = unknown>(response: string): T;
}
